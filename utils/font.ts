import { Open_Sans, Permanent_Marker, Rubik_Dirt } from 'next/font/google';

export const openSans = Satisfy({
    weight: '400',
    subsets: ['latin'],
    display: 'optional',
})

export const rubik_dirt = Rubik_Dirt({
    weight: '400',
    subsets: ['latin'],
    display: 'optional',
})

export const perm_marker = Permanent_Marker({
    weight: '400',
    subsets: ['latin'],
    display: 'optional',
})